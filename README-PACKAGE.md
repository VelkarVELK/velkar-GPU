Velkar miner source package

Build on Windows:
  cargo build --release -p velkar-cpuminer --manifest-path Cargo.toml

Build on Linux:
  cargo build --release -p velkar-cpuminer --manifest-path Cargo.toml

This package contains source only. No binaries or data files are included.
